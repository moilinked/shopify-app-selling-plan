services:
  shopify-app-auth:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: shopify-app-auth
    ports:
      - "${PORT:-9998}:9998"
    volumes:
      - ./config:/app/config:ro
    restart: unless-stopped
